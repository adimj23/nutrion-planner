This app helps users set nutritional goals, generates daily meal plans that meet calorie and macro constraints, and automatically produces a grocery list for a chosen time horizon.

Tech Stack:

Django
Django REST Framework
PostgreSQL
Docker + Docker Compose
Celery (later)
Redis (later)